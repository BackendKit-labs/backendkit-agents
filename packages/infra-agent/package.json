{
  "name": "@backendkit-labs/infra-agent",
  "version": "0.3.0",
  "description": "Infrastructure agent — Docker, Compose, Swarm, Volumes, Containerd, Kubernetes. Runs as MCP stdio plugin or standalone HTTP MCP server.",
  "main": "dist/mcp.js",
  "bin": {
    "infra-agent": "./dist/mcp.js"
  },
  "files": [
    "dist/",
    "AGENT.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BackendKit-labs/backendkit-agents.git",
    "directory": "packages/infra-agent"
  },
  "publishConfig": { "access": "public" },
  "license": "MIT",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "start": "node dist/mcp.js",
    "start:http": "node dist/mcp.js http",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@backendkit-labs/agent-core": "^0.17.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "dockerode": "^4.0.4",
    "dotenv": "^16.4.7",
    "openai": "^6.40.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/dockerode": "^4.0.1",
    "@types/node": "^20.17.12",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
