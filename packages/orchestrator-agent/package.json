{
  "name": "@backendkit-labs/orchestrator-agent",
  "version": "0.1.4",
  "description": "Config-driven orchestration agent — decomposes tasks and coordinates specialist agents via shared vault and configurable LLM providers.",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "orchestrator-agent": "./dist/server.js"
  },
  "files": [
    "dist/",
    "AGENT.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BackendKit-labs/backendkit-agents.git",
    "directory": "packages/orchestrator-agent"
  },
  "publishConfig": { "access": "public" },
  "license": "MIT",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "build": "tsc --noCheck",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@backendkit-labs/agent-core": "^0.22.2",
    "@backendkit-labs/agent-enterprise": "^0.3.6",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "openai": "^4.0.0",
    "yaml": "^2.4.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.12",
    "typescript": "^5.7.3"
  }
}
