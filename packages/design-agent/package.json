{
  "name": "@backendkit-labs/design-agent",
  "version": "0.1.0",
  "description": "Spec-driven design lifecycle MCP plugin — roadmap, phases (spec→implement→verify), multi-project overview.",
  "main": "dist/server.js",
  "bin": {
    "design-agent": "./dist/server.js"
  },
  "files": [
    "dist/",
    "AGENT.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BackendKit-labs/backendkit-agents.git",
    "directory": "packages/design-agent"
  },
  "publishConfig": { "access": "public" },
  "license": "MIT",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@backendkit-labs/agent-core": "^0.20.2",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.12",
    "typescript": "^5.7.3"
  }
}
